import { ActivityIndicator, View } from 'react-native';

function SectionFooterSpinner() {
  return (
    <View className="py-4">
      <ActivityIndicator />
    </View>
  );
}

export default SectionFooterSpinner;
